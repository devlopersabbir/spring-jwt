
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_jwt?useSSL=false
    username: root
    jpa:
      hibernate:
      ddl-auto: create-drop
      show-sql: true
      properties:
        format_sql: true
server:
  port: 5000